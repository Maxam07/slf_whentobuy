<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SFL When-to-Buy</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link href="/styles/main.css" rel="stylesheet">
</head>
    <body>
        <div class="container py-2">
            <div class="date_time">
                <span>       
                    Last update: <%= locals.updateTime %>
                </span>
                <span>
                    <%= locals.date %>
                </span>
            </div>

             <table class="sortable listtable p-3 col-12">
                <tr>
                    <th class="px-3 bg-body-secondary">Name</th>
                    <th class="px-3 bg-body-secondary">P2P Floor Price</th>
                    <th class="px-3 bg-body-secondary"># RCS</th>
                    <th class="px-3 bg-body-secondary">Cost</th>
                    <th class="px-3 bg-body-secondary">Cost x 4</th>
                    <th class="px-3 bg-body-secondary">Buy or No</th>
                </tr>
                <% Object.keys(data).forEach((key, index) => { %>
                    <tr>
                        <td class="text-start"><%= key %></td>
                        <td><%= Object.values(locals.data)[index] %></td>
                        <td><%= numResourses[index] %></td>
                        <td><%=(Object.values(locals.data)[index] * locals.numResourses[index]).toFixed(2) %></td>
                        <td><%=((Object.values(locals.data)[index] * locals.numResourses[index]).toFixed(2))*4 %></td>
                        <% if (((Object.values(locals.data)[index] * locals.numResourses[index]).toFixed(2))*4 < 10) { %>
                            <td class="bg-primary">Buy</td>
                        <% } else { %>
                            <td class="bg-primary-subtle">NO</td>
                        <% } %>
                    </tr>
                <% }); %>
            </table>
        </div>
    </body>
</html>
